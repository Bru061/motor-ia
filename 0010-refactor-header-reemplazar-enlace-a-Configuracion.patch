From 7d992d0934508cf8b6d5436fc5a89af295f981ca Mon Sep 17 00:00:00 2001
From: Bru <bru@motoria.local>
Date: Fri, 24 Jul 2026 20:34:20 +0000
Subject: [PATCH 1/2] refactor(header): reemplazar enlace a Configuracion por
 toggle directo de tema

---
 frontend/src/components/layout/Header.jsx | 27 +++++++++++++----------
 1 file changed, 15 insertions(+), 12 deletions(-)

diff --git a/frontend/src/components/layout/Header.jsx b/frontend/src/components/layout/Header.jsx
index 7117125..4097008 100644
--- a/frontend/src/components/layout/Header.jsx
+++ b/frontend/src/components/layout/Header.jsx
@@ -4,18 +4,16 @@ import {
   FiChevronDown,
   FiLogOut,
   FiMenu,
-  FiSettings,
+  FiMoon,
+  FiSun,
   FiUser,
 } from "react-icons/fi";
 import UserAvatar from "../ui/UserAvatar";
 import useAuth from "../../hooks/useAuth";
 import useSidebar from "../../hooks/useSidebar";
+import useTheme from "../../hooks/useTheme";
 import useToast from "../../hooks/useToast";
-import {
-  getUserDisplayName,
-  getUserProfilePath,
-  getUserSettingsPath,
-} from "../../utils/user";
+import { getUserDisplayName, getUserProfilePath } from "../../utils/user";
 import "../../styles/Layout.css";
 
 function Header() {
@@ -23,9 +21,11 @@ function Header() {
   const toast = useToast();
   const { logout, role, user } = useAuth();
   const { toggle: toggleSidebar } = useSidebar();
+  const { theme, toggleTheme } = useTheme();
   const [isMenuOpen, setIsMenuOpen] = useState(false);
   const menuRef = useRef(null);
   const displayName = getUserDisplayName(user);
+  const isLight = theme === "light";
 
   useEffect(() => {
     if (!isMenuOpen) {
@@ -99,14 +99,17 @@ function Header() {
                 <span>Mi Perfil</span>
               </Link>
             )}
-            <Link
-              to={getUserSettingsPath(role)}
+            <button
+              type="button"
               role="menuitem"
-              onClick={() => setIsMenuOpen(false)}
+              onClick={() => {
+                toggleTheme();
+                setIsMenuOpen(false);
+              }}
             >
-              <FiSettings aria-hidden="true" />
-              <span>Configuración</span>
-            </Link>
+              {isLight ? <FiMoon aria-hidden="true" /> : <FiSun aria-hidden="true" />}
+              <span>{isLight ? "Tema oscuro" : "Tema claro"}</span>
+            </button>
             <button type="button" role="menuitem" onClick={handleLogout}>
               <FiLogOut aria-hidden="true" />
               <span>Cerrar sesión</span>
-- 
2.43.0

