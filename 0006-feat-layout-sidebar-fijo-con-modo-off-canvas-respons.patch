From 12ec9196413f6bb775603a1993872ec1cc18d371 Mon Sep 17 00:00:00 2001
From: Bru <bru@motoria.local>
Date: Fri, 24 Jul 2026 02:19:49 +0000
Subject: [PATCH 1/3] feat(layout): sidebar fijo con modo off-canvas responsivo
 y boton toggle

---
 frontend/src/components/layout/Header.jsx  | 12 ++++
 frontend/src/components/layout/Sidebar.jsx | 66 +++++++++++++--------
 frontend/src/context/SidebarContext.jsx    | 28 +++++++++
 frontend/src/context/sidebarContext.js     |  3 +
 frontend/src/hooks/useSidebar.js           | 14 +++++
 frontend/src/layouts/AdminLayout.jsx       | 19 +++---
 frontend/src/layouts/StudentLayout.jsx     | 19 +++---
 frontend/src/styles/Layout.css             | 69 +++++++++++++++-------
 8 files changed, 168 insertions(+), 62 deletions(-)
 create mode 100644 frontend/src/context/SidebarContext.jsx
 create mode 100644 frontend/src/context/sidebarContext.js
 create mode 100644 frontend/src/hooks/useSidebar.js

diff --git a/frontend/src/components/layout/Header.jsx b/frontend/src/components/layout/Header.jsx
index edbdd97..7117125 100644
--- a/frontend/src/components/layout/Header.jsx
+++ b/frontend/src/components/layout/Header.jsx
@@ -3,11 +3,13 @@ import { Link, useNavigate } from "react-router-dom";
 import {
   FiChevronDown,
   FiLogOut,
+  FiMenu,
   FiSettings,
   FiUser,
 } from "react-icons/fi";
 import UserAvatar from "../ui/UserAvatar";
 import useAuth from "../../hooks/useAuth";
+import useSidebar from "../../hooks/useSidebar";
 import useToast from "../../hooks/useToast";
 import {
   getUserDisplayName,
@@ -20,6 +22,7 @@ function Header() {
   const navigate = useNavigate();
   const toast = useToast();
   const { logout, role, user } = useAuth();
+  const { toggle: toggleSidebar } = useSidebar();
   const [isMenuOpen, setIsMenuOpen] = useState(false);
   const menuRef = useRef(null);
   const displayName = getUserDisplayName(user);
@@ -59,6 +62,15 @@ function Header() {
 
   return (
     <header className="header">
+      <button
+        className="header__menu-toggle"
+        type="button"
+        aria-label="Mostrar u ocultar menú de navegación"
+        onClick={toggleSidebar}
+      >
+        <FiMenu aria-hidden="true" />
+      </button>
+
       <div className="header__spacer" />
 
       <div className="header-user-menu" ref={menuRef}>
diff --git a/frontend/src/components/layout/Sidebar.jsx b/frontend/src/components/layout/Sidebar.jsx
index 8ee2f58..2fcf65a 100644
--- a/frontend/src/components/layout/Sidebar.jsx
+++ b/frontend/src/components/layout/Sidebar.jsx
@@ -8,9 +8,12 @@ import {
   FiMap,
   FiUsers,
 } from "react-icons/fi";
+import useSidebar from "../../hooks/useSidebar";
 import "../../styles/Layout.css";
 
 function Sidebar({ type = "student" }) {
+  const { isOpen, close } = useSidebar();
+
   const studentLinks = [
     {
       to: "/dashboard",
@@ -50,34 +53,45 @@ function Sidebar({ type = "student" }) {
   const links = type === "admin" ? adminLinks : studentLinks;
 
   return (
-    <aside className="sidebar">
-      <div className="sidebar__brand">
-        <FiGitBranch aria-hidden="true" />
-        <h2>Motor IA</h2>
-      </div>
+    <>
+      {isOpen && (
+        <button
+          className="sidebar-backdrop"
+          type="button"
+          aria-label="Cerrar menú de navegación"
+          onClick={close}
+        />
+      )}
+
+      <aside className={isOpen ? "sidebar sidebar--open" : "sidebar"}>
+        <div className="sidebar__brand">
+          <FiGitBranch aria-hidden="true" />
+          <h2>Motor IA</h2>
+        </div>
 
-      <nav className="sidebar__nav" aria-label="Navegación principal">
-        {links.map((link) => {
-          const Icon = link.icon;
+        <nav className="sidebar__nav" aria-label="Navegación principal">
+          {links.map((link) => {
+            const Icon = link.icon;
 
-          return (
-            <NavLink
-              key={link.to}
-              to={link.to}
-              end
-              className={({ isActive }) =>
-                isActive ? "sidebar__link sidebar__link--active" : "sidebar__link"
-              }
-            >
-              <span className="sidebar__icon">
-                <Icon aria-hidden="true" focusable="false" />
-              </span>
-              <span>{link.label}</span>
-            </NavLink>
-          );
-        })}
-      </nav>
-    </aside>
+            return (
+              <NavLink
+                key={link.to}
+                to={link.to}
+                end
+                className={({ isActive }) =>
+                  isActive ? "sidebar__link sidebar__link--active" : "sidebar__link"
+                }
+              >
+                <span className="sidebar__icon">
+                  <Icon aria-hidden="true" focusable="false" />
+                </span>
+                <span>{link.label}</span>
+              </NavLink>
+            );
+          })}
+        </nav>
+      </aside>
+    </>
   );
 }
 
diff --git a/frontend/src/context/SidebarContext.jsx b/frontend/src/context/SidebarContext.jsx
new file mode 100644
index 0000000..47ad83b
--- /dev/null
+++ b/frontend/src/context/SidebarContext.jsx
@@ -0,0 +1,28 @@
+import { useCallback, useEffect, useMemo, useState } from "react";
+import { useLocation } from "react-router-dom";
+import { SidebarContext } from "./sidebarContext";
+
+function SidebarProvider({ children }) {
+  const [isOpen, setIsOpen] = useState(false);
+  const location = useLocation();
+
+  // Cierra el sidebar automáticamente al navegar a otra ruta,
+  // para que no se quede abierto tapando la pantalla en móvil.
+  useEffect(() => {
+    setIsOpen(false);
+  }, [location.pathname]);
+
+  const toggle = useCallback(() => {
+    setIsOpen((current) => !current);
+  }, []);
+
+  const close = useCallback(() => {
+    setIsOpen(false);
+  }, []);
+
+  const value = useMemo(() => ({ isOpen, toggle, close }), [isOpen, toggle, close]);
+
+  return <SidebarContext.Provider value={value}>{children}</SidebarContext.Provider>;
+}
+
+export default SidebarProvider;
diff --git a/frontend/src/context/sidebarContext.js b/frontend/src/context/sidebarContext.js
new file mode 100644
index 0000000..7fb3983
--- /dev/null
+++ b/frontend/src/context/sidebarContext.js
@@ -0,0 +1,3 @@
+import { createContext } from "react";
+
+export const SidebarContext = createContext(null);
diff --git a/frontend/src/hooks/useSidebar.js b/frontend/src/hooks/useSidebar.js
new file mode 100644
index 0000000..9f5ed43
--- /dev/null
+++ b/frontend/src/hooks/useSidebar.js
@@ -0,0 +1,14 @@
+import { useContext } from "react";
+import { SidebarContext } from "../context/sidebarContext";
+
+function useSidebar() {
+  const context = useContext(SidebarContext);
+
+  if (!context) {
+    throw new Error("useSidebar debe usarse dentro de SidebarProvider");
+  }
+
+  return context;
+}
+
+export default useSidebar;
diff --git a/frontend/src/layouts/AdminLayout.jsx b/frontend/src/layouts/AdminLayout.jsx
index e8c6636..300610a 100644
--- a/frontend/src/layouts/AdminLayout.jsx
+++ b/frontend/src/layouts/AdminLayout.jsx
@@ -1,21 +1,24 @@
 import { Outlet } from "react-router-dom";
 import Sidebar from "../components/layout/Sidebar";
 import Header from "../components/layout/Header";
+import SidebarProvider from "../context/SidebarContext";
 import "../styles/Layout.css";
 
 function AdminLayout() {
   return (
-    <div className="app-layout">
-      <Sidebar type="admin" />
+    <SidebarProvider>
+      <div className="app-layout">
+        <Sidebar type="admin" />
 
-      <div className="layout-content">
-        <Header />
+        <div className="layout-content">
+          <Header />
 
-        <main className="main-content">
-          <Outlet />
-        </main>
+          <main className="main-content">
+            <Outlet />
+          </main>
+        </div>
       </div>
-    </div>
+    </SidebarProvider>
   );
 }
 
diff --git a/frontend/src/layouts/StudentLayout.jsx b/frontend/src/layouts/StudentLayout.jsx
index 70bb2c2..b84898e 100644
--- a/frontend/src/layouts/StudentLayout.jsx
+++ b/frontend/src/layouts/StudentLayout.jsx
@@ -1,21 +1,24 @@
 import { Outlet } from "react-router-dom";
 import Sidebar from "../components/layout/Sidebar";
 import Header from "../components/layout/Header";
+import SidebarProvider from "../context/SidebarContext";
 import "../styles/Layout.css";
 
 function StudentLayout() {
   return (
-    <div className="app-layout">
-      <Sidebar type="student" />
+    <SidebarProvider>
+      <div className="app-layout">
+        <Sidebar type="student" />
 
-      <div className="layout-content">
-        <Header />
+        <div className="layout-content">
+          <Header />
 
-        <main className="main-content">
-          <Outlet />
-        </main>
+          <main className="main-content">
+            <Outlet />
+          </main>
+        </div>
       </div>
-    </div>
+    </SidebarProvider>
   );
 }
 
diff --git a/frontend/src/styles/Layout.css b/frontend/src/styles/Layout.css
index e2d5a85..a0defbd 100644
--- a/frontend/src/styles/Layout.css
+++ b/frontend/src/styles/Layout.css
@@ -1,20 +1,22 @@
 .app-layout {
   min-height: 100vh;
-  display: grid;
-  grid-template-columns: 240px minmax(0, 1fr);
   background: var(--bg);
 }
 
 .sidebar {
-  position: sticky;
+  position: fixed;
   top: 0;
-  min-height: 100vh;
+  left: 0;
+  bottom: 0;
+  width: 240px;
+  z-index: 30;
   padding: 28px 16px 24px;
   display: flex;
   flex-direction: column;
   background: #09111f;
   border-right: 1px solid #142035;
   color: var(--muted-strong);
+  overflow-y: auto;
 }
 
 .sidebar__brand {
@@ -92,6 +94,7 @@
 
 .layout-content {
   min-width: 0;
+  margin-left: 240px;
   display: flex;
   flex-direction: column;
 }
@@ -107,6 +110,34 @@
   border-bottom: 1px solid #17243a;
 }
 
+.header__menu-toggle {
+  width: 40px;
+  height: 40px;
+  display: none;
+  align-items: center;
+  justify-content: center;
+  border: 1px solid #223049;
+  border-radius: 8px;
+  background: #111d31;
+  color: var(--muted-strong);
+  font-size: 18px;
+}
+
+.header__menu-toggle:hover {
+  color: var(--cyan);
+  border-color: rgba(16, 216, 242, 0.55);
+}
+
+.sidebar-backdrop {
+  position: fixed;
+  inset: 0;
+  z-index: 25;
+  border: 0;
+  padding: 0;
+  background: rgba(3, 7, 14, 0.6);
+  cursor: pointer;
+}
+
 .header__spacer {
   flex: 1;
 }
@@ -384,30 +415,28 @@
 }
 
 @media (max-width: 980px) {
-  .app-layout {
-    grid-template-columns: 1fr;
+  .layout-content {
+    margin-left: 0;
   }
 
-  .sidebar {
-    position: static;
-    min-height: auto;
-    padding: 18px;
+  .header__menu-toggle {
+    display: flex;
   }
 
-  .sidebar__nav {
-    margin-top: 18px;
-    padding-bottom: 16px;
-    flex-direction: row;
-    overflow-x: auto;
+  .sidebar {
+    width: 260px;
+    transform: translateX(-100%);
+    box-shadow: var(--shadow);
+    transition: transform 0.22s ease;
   }
 
-  .sidebar__link {
-    width: auto;
-    flex: 0 0 auto;
+  .sidebar--open {
+    transform: translateX(0);
   }
 
-  .sidebar__footer {
-    display: none;
+  .sidebar__nav {
+    margin-top: 18px;
+    padding-bottom: 16px;
   }
 
   .main-content {
-- 
2.43.0

