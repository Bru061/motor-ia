From 2921246e650f3abf5d9db829ccb5a383a41fee3a Mon Sep 17 00:00:00 2001
From: Bru <bru@motoria.local>
Date: Fri, 24 Jul 2026 20:34:20 +0000
Subject: [PATCH 2/2] fix(theme): migrar colores fijos a variables para que el
 tema claro/oscuro aplique en toda la app

---
 frontend/src/styles/Auth.css      |  36 +++----
 frontend/src/styles/Layout.css    |  76 ++++----------
 frontend/src/styles/Pages.css     | 166 +++++++++++++++---------------
 frontend/src/styles/dashboard.css |  22 ++--
 frontend/src/styles/global.css    |  18 +++-
 frontend/src/styles/perfil.css    |  44 ++++----
 frontend/src/styles/ruta.css      | 132 ++++++++++++------------
 frontend/src/styles/ui.css        |  30 +++---
 8 files changed, 248 insertions(+), 276 deletions(-)

diff --git a/frontend/src/styles/Auth.css b/frontend/src/styles/Auth.css
index 58ae8a5..d0820a8 100644
--- a/frontend/src/styles/Auth.css
+++ b/frontend/src/styles/Auth.css
@@ -8,8 +8,8 @@
   display: grid;
   grid-template-columns: 1fr 1fr;
   gap: 10px;
-  border-bottom: 1px solid #1d2b42;
-  color: #657187;
+  border-bottom: 1px solid var(--border);
+  color: var(--muted);
   font-size: 14px;
   font-weight: 800;
   text-align: center;
@@ -47,22 +47,22 @@
   width: 100%;
   min-height: 48px;
   padding: 0 14px;
-  border: 1px solid #1d2a3d;
+  border: 1px solid var(--border);
   border-radius: 8px;
   outline: none;
-  background: #070d18;
+  background: var(--bg-deep);
   color: var(--text);
   font-size: 15px;
 }
 
 .auth-form__group input::placeholder {
-  color: #778298;
+  color: var(--muted);
   font-weight: 600;
 }
 
 .auth-form__group input:focus {
   border-color: var(--cyan);
-  box-shadow: 0 0 0 3px rgba(16, 216, 242, 0.12);
+  box-shadow: 0 0 0 3px rgba(var(--cyan-rgb), 0.12);
 }
 
 .auth-floating-field {
@@ -73,10 +73,10 @@
   width: 100%;
   min-height: 54px;
   padding: 22px 14px 8px;
-  border: 1px solid #1d2a3d;
+  border: 1px solid var(--border);
   border-radius: 8px;
   outline: none;
-  background: #070d18;
+  background: var(--bg-deep);
   color: var(--text);
   font-size: 15px;
   transition: border-color 0.15s ease, box-shadow 0.15s ease;
@@ -90,7 +90,7 @@
   position: absolute;
   top: 17px;
   left: 15px;
-  color: #778298;
+  color: var(--muted);
   font-size: 15px;
   font-weight: 600;
   pointer-events: none;
@@ -111,7 +111,7 @@
 
 .auth-floating-field input:focus {
   border-color: var(--cyan);
-  box-shadow: 0 0 0 3px rgba(16, 216, 242, 0.12);
+  box-shadow: 0 0 0 3px rgba(var(--cyan-rgb), 0.12);
 }
 
 .auth-floating-field input:-webkit-autofill {
@@ -129,7 +129,7 @@
   place-items: center;
   border: 0;
   background: transparent;
-  color: #778298;
+  color: var(--muted);
   font-size: 18px;
 }
 
@@ -144,7 +144,7 @@
   border: 0;
   border-radius: 8px;
   background: var(--cyan);
-  color: #02121a;
+  color: var(--on-accent);
   font-size: 14px;
   font-weight: 900;
 }
@@ -162,7 +162,7 @@
   border: 1px solid rgba(255, 107, 122, 0.42);
   border-radius: 8px;
   background: rgba(255, 107, 122, 0.1);
-  color: #ffb6bf;
+  color: var(--danger);
   font-size: 14px;
   font-weight: 700;
 }
@@ -172,7 +172,7 @@
   margin: 24px 0 26px;
   border: 0;
   background: transparent;
-  color: #657187;
+  color: var(--muted);
   font-size: 14px;
 }
 
@@ -184,7 +184,7 @@
   position: relative;
   display: flex;
   justify-content: center;
-  color: #657187;
+  color: var(--muted);
   font-size: 12px;
 }
 
@@ -195,7 +195,7 @@
   top: 50%;
   width: 37%;
   height: 1px;
-  background: #1d2b42;
+  background: var(--border);
 }
 
 .auth-separator::before {
@@ -221,9 +221,9 @@
   align-items: center;
   justify-content: center;
   gap: 10px;
-  border: 1px solid #1d2a3d;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #070d18;
+  background: var(--bg-deep);
   color: var(--text);
   font-weight: 800;
 }
diff --git a/frontend/src/styles/Layout.css b/frontend/src/styles/Layout.css
index a0defbd..a17ced0 100644
--- a/frontend/src/styles/Layout.css
+++ b/frontend/src/styles/Layout.css
@@ -13,8 +13,8 @@
   padding: 28px 16px 24px;
   display: flex;
   flex-direction: column;
-  background: #09111f;
-  border-right: 1px solid #142035;
+  background: var(--panel-strong);
+  border-right: 1px solid var(--border);
   color: var(--muted-strong);
   overflow-y: auto;
 }
@@ -45,7 +45,7 @@
   display: flex;
   flex-direction: column;
   gap: 8px;
-  border-bottom: 1px solid #1a2740;
+  border-bottom: 1px solid var(--border);
 }
 
 .sidebar__link,
@@ -68,12 +68,12 @@
 
 .sidebar__link:hover,
 .sidebar__action:hover {
-  background: #111d31;
+  background: var(--panel-soft);
   color: var(--text);
 }
 
 .sidebar__link--active {
-  background: #10213a;
+  background: rgba(var(--cyan-rgb), 0.14);
   color: var(--cyan);
 }
 
@@ -106,8 +106,8 @@
   align-items: center;
   justify-content: flex-end;
   gap: 10px;
-  background: #0a1322;
-  border-bottom: 1px solid #17243a;
+  background: var(--panel-strong);
+  border-bottom: 1px solid var(--border);
 }
 
 .header__menu-toggle {
@@ -116,16 +116,16 @@
   display: none;
   align-items: center;
   justify-content: center;
-  border: 1px solid #223049;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #111d31;
+  background: var(--panel-soft);
   color: var(--muted-strong);
   font-size: 18px;
 }
 
 .header__menu-toggle:hover {
   color: var(--cyan);
-  border-color: rgba(16, 216, 242, 0.55);
+  border-color: rgba(var(--cyan-rgb), 0.55);
 }
 
 .sidebar-backdrop {
@@ -154,19 +154,19 @@
   gap: 10px;
   border: 1px solid transparent;
   border-radius: 999px;
-  background: #111d31;
+  background: var(--panel-soft);
   color: var(--text);
   font-size: 13px;
   transition: border-color 0.15s ease, background 0.15s ease;
 }
 
 .header-user-menu__trigger:hover {
-  border-color: rgba(16, 216, 242, 0.35);
+  border-color: rgba(var(--cyan-rgb), 0.35);
 }
 
 .header-user-menu__trigger[aria-expanded="true"] {
-  border-color: rgba(16, 216, 242, 0.55);
-  background: #16233a;
+  border-color: rgba(var(--cyan-rgb), 0.55);
+  background: var(--panel);
 }
 
 .header-user-menu__trigger strong {
@@ -197,7 +197,7 @@
   display: flex;
   flex-direction: column;
   gap: 2px;
-  border: 1px solid #223049;
+  border: 1px solid var(--border);
   border-radius: 10px;
   background: var(--panel-strong);
   box-shadow: var(--shadow);
@@ -250,42 +250,6 @@
   background: rgba(255, 107, 122, 0.12);
 }
 
-.header__profile {
-  min-width: 156px;
-  height: 44px;
-  padding: 0 18px;
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  gap: 12px;
-  border-radius: 999px;
-  background: #19263c;
-  color: var(--text);
-  font-size: 13px;
-}
-
-.header__initials {
-  color: #718095;
-  font-weight: 800;
-}
-
-.header__button {
-  width: 44px;
-  height: 44px;
-  display: inline-flex;
-  align-items: center;
-  justify-content: center;
-  border: 1px solid #223049;
-  border-radius: 999px;
-  background: #111d31;
-  color: var(--muted-strong);
-}
-
-.header__button:hover {
-  color: var(--cyan);
-  border-color: rgba(16, 216, 242, 0.55);
-}
-
 .main-content {
   flex: 1;
   min-width: 0;
@@ -317,7 +281,7 @@
   display: flex;
   align-items: center;
   overflow: hidden;
-  background: #101b2f;
+  background: var(--bg-deep);
 }
 
 .public-hero__content {
@@ -402,13 +366,13 @@
   display: grid;
   place-items: center;
   padding: 32px;
-  background: #070c16;
+  background: var(--bg);
 }
 
 .public-card {
   width: min(430px, 100%);
   padding: 40px;
-  border: 1px solid #15243a;
+  border: 1px solid var(--border);
   border-radius: 8px;
   background: var(--panel-strong);
   box-shadow: var(--shadow);
@@ -463,10 +427,6 @@
     padding: 0 16px;
   }
 
-  .header__profile {
-    min-width: 0;
-  }
-
   .public-card {
     padding: 28px 20px;
   }
diff --git a/frontend/src/styles/Pages.css b/frontend/src/styles/Pages.css
index 398b88d..3ed8886 100644
--- a/frontend/src/styles/Pages.css
+++ b/frontend/src/styles/Pages.css
@@ -47,7 +47,7 @@
 .admin-panel,
 .assessment-shell,
 .module-detail {
-  border: 1px solid #18263d;
+  border: 1px solid var(--border);
   border-radius: 8px;
   background: var(--panel);
   box-shadow: 0 16px 36px rgba(0, 0, 0, 0.12);
@@ -71,8 +71,8 @@
   display: grid;
   place-items: center;
   border-radius: 8px;
-  background: #0d1728;
-  color: #020711;
+  background: var(--panel-strong);
+  color: var(--bg-deep);
   font-size: 24px;
   font-weight: 900;
 }
@@ -130,7 +130,7 @@
   display: grid;
   place-items: center;
   border-radius: 50%;
-  background: conic-gradient(var(--cyan) var(--value), #26354e 0);
+  background: conic-gradient(var(--cyan) var(--value), var(--border) 0);
 }
 
 .progress-ring::before {
@@ -166,7 +166,7 @@
   display: grid;
   place-items: center;
   border-radius: 8px;
-  background: #101a2d;
+  background: var(--panel-strong);
   color: var(--cyan);
   font-size: 18px;
 }
@@ -227,7 +227,7 @@
 }
 
 .section-heading span {
-  color: #758298;
+  color: var(--muted);
   font-size: 12px;
 }
 
@@ -246,7 +246,7 @@
   align-items: center;
   gap: 14px;
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
 }
 
 .activity-item > span {
@@ -262,7 +262,7 @@
 .activity-item small {
   display: block;
   margin-top: 4px;
-  color: #79869c;
+  color: var(--muted);
 }
 
 .quick-card h2 {
@@ -281,7 +281,7 @@
   gap: 12px;
   border: 0;
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
   color: var(--muted-strong);
   font-weight: 800;
 }
@@ -296,7 +296,7 @@
   padding: 16px;
   display: grid;
   gap: 6px;
-  border: 1px solid #24334d;
+  border: 1px solid var(--border);
   border-radius: 8px;
 }
 
@@ -332,19 +332,19 @@
 .stepper__item {
   display: grid;
   gap: 8px;
-  color: #6d7a90;
+  color: var(--muted);
   font-size: 11px;
   font-weight: 800;
 }
 
 .stepper__item span {
-  color: #738096;
+  color: var(--muted);
 }
 
 .stepper__bar {
   height: 6px;
   border-radius: 999px;
-  background: #233149;
+  background: var(--border);
 }
 
 .stepper__bar.is-active {
@@ -369,9 +369,9 @@
   display: grid;
   place-items: center;
   gap: 10px;
-  border: 1px solid #1c2b43;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #142037;
+  background: var(--border);
   color: var(--muted-strong);
   text-align: center;
 }
@@ -402,7 +402,7 @@
 
 .experience-card--active {
   border-color: var(--cyan);
-  box-shadow: 0 0 0 1px rgba(16, 216, 242, 0.28);
+  box-shadow: 0 0 0 1px rgba(var(--cyan-rgb), 0.28);
 }
 
 .assessment-block {
@@ -425,10 +425,10 @@
   width: 100%;
   min-height: 50px;
   padding: 0 16px;
-  border: 1px solid #1d2b43;
+  border: 1px solid var(--border);
   border-radius: 8px;
   outline: none;
-  background: #142037;
+  background: var(--border);
   color: var(--text);
 }
 
@@ -445,9 +445,9 @@
 .tech-panel {
   min-height: 132px;
   padding: 18px;
-  border: 1px solid #1d2b43;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #142037;
+  background: var(--border);
 }
 
 .tech-panel h3 {
@@ -475,7 +475,7 @@
 
 .tech-panel--yellow h3,
 .tech-panel--yellow .tech-tag--selected {
-  color: #f5d84c;
+  color: var(--amber);
 }
 
 .tech-tags {
@@ -504,7 +504,7 @@
 .ghost-action {
   border: 0;
   background: transparent;
-  color: #7a879d;
+  color: var(--muted);
   font-size: 12px;
   font-weight: 800;
 }
@@ -519,7 +519,7 @@
   border: 0;
   border-radius: 8px;
   background: var(--cyan);
-  color: #031017;
+  color: var(--bg-deep);
   font-weight: 900;
 }
 
@@ -553,11 +553,11 @@
   width: 8px;
   height: 8px;
   border-radius: 50%;
-  background: #5d697d;
+  background: var(--muted);
 }
 
 .dot--active {
-  background: #3486ff;
+  background: var(--blue);
 }
 
 .dot--completed {
@@ -586,7 +586,7 @@
   left: 50%;
   width: 6px;
   transform: translateX(-50%);
-  background: #3a4659;
+  background: var(--muted);
 }
 
 .roadmap-node-wrap {
@@ -600,7 +600,7 @@
 .roadmap-level {
   padding: 8px 14px;
   border-radius: 8px;
-  background: #13243a;
+  background: var(--border);
   color: var(--cyan);
   font-size: 11px;
   font-weight: 900;
@@ -613,18 +613,18 @@
   display: flex;
   align-items: center;
   gap: 12px;
-  border: 1px solid #2a3a55;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #121e32;
+  background: var(--border);
 }
 
 .roadmap-node--completed {
-  border-color: #09a875;
-  background: #063322;
+  border-color: var(--green);
+  background: rgba(var(--green-rgb), 0.16);
 }
 
 .roadmap-node--active {
-  border-color: #3486ff;
+  border-color: var(--blue);
 }
 
 .roadmap-node--pending {
@@ -638,17 +638,17 @@
   display: grid;
   place-items: center;
   border-radius: 8px;
-  background: #435064;
-  color: #dbe4ef;
+  background: var(--muted);
+  color: var(--on-muted);
 }
 
 .roadmap-node--completed .roadmap-node__icon {
   background: var(--green);
-  color: #032015;
+  color: var(--bg-deep);
 }
 
 .roadmap-node--active .roadmap-node__icon {
-  background: #2f7df3;
+  background: var(--blue);
 }
 
 .roadmap-node strong {
@@ -678,7 +678,7 @@
 }
 
 .module-detail__top {
-  color: #728098;
+  color: var(--muted);
   font-size: 11px;
   font-weight: 900;
 }
@@ -695,7 +695,7 @@
   display: grid;
   place-items: center;
   border-radius: 8px;
-  background: #2f7df3;
+  background: var(--blue);
 }
 
 .module-detail h2,
@@ -729,14 +729,14 @@
   display: block;
   overflow: hidden;
   border-radius: 999px;
-  background: #26354e;
+  background: var(--border);
 }
 
 .module-progress b {
   width: 25%;
   height: 100%;
   display: block;
-  background: #3486ff;
+  background: var(--blue);
 }
 
 .resource-list {
@@ -758,7 +758,7 @@
 }
 
 .resource-list input {
-  accent-color: #3486ff;
+  accent-color: var(--blue);
 }
 
 .module-actions {
@@ -821,9 +821,9 @@
   display: inline-flex;
   align-items: center;
   gap: 8px;
-  border: 1px solid #263650;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
   color: var(--text);
   font-size: 13px;
   font-weight: 800;
@@ -858,7 +858,7 @@
   margin: 0;
   padding: 6px;
   list-style: none;
-  border: 1px solid #263650;
+  border: 1px solid var(--border);
   border-radius: 8px;
   background: var(--panel-strong);
   box-shadow: var(--shadow);
@@ -898,9 +898,9 @@
   display: inline-flex;
   align-items: center;
   gap: 8px;
-  border: 1px solid #263650;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
   color: var(--muted-strong);
   font-weight: 800;
 }
@@ -920,7 +920,7 @@
   gap: 8px;
   border: 0;
   border-radius: 8px;
-  background: #17243a;
+  background: var(--border);
   color: var(--muted-strong);
   font-weight: 800;
 }
@@ -955,7 +955,7 @@
 
 .users-table__head {
   min-height: 48px;
-  color: #66738b;
+  color: var(--muted);
   font-size: 11px;
   font-weight: 900;
 }
@@ -966,7 +966,7 @@
 
 .users-table__row {
   min-height: 70px;
-  border-top: 1px solid #18263d;
+  border-top: 1px solid var(--border);
   color: var(--muted-strong);
   font-size: 13px;
 }
@@ -1029,7 +1029,7 @@
   height: 7px;
   overflow: hidden;
   border-radius: 999px;
-  background: #26354e;
+  background: var(--border);
 }
 
 .table-progress i::after {
@@ -1076,8 +1076,8 @@
 }
 
 .admin-badge--role {
-  border-color: rgba(16, 216, 242, 0.28);
-  background: rgba(16, 216, 242, 0.08);
+  border-color: rgba(var(--cyan-rgb), 0.28);
+  background: rgba(var(--cyan-rgb), 0.08);
   color: var(--cyan);
 }
 
@@ -1100,9 +1100,9 @@
   align-items: center;
   justify-content: center;
   gap: 7px;
-  border: 1px solid rgba(16, 216, 242, 0.28);
+  border: 1px solid rgba(var(--cyan-rgb), 0.28);
   border-radius: 8px;
-  background: rgba(16, 216, 242, 0.06);
+  background: rgba(var(--cyan-rgb), 0.06);
   color: var(--cyan);
   font-size: 12px;
   font-weight: 850;
@@ -1112,7 +1112,7 @@
 .admin-row-action:focus-visible,
 .admin-clear-button:hover,
 .admin-clear-button:focus-visible {
-  border-color: rgba(16, 216, 242, 0.7);
+  border-color: rgba(var(--cyan-rgb), 0.7);
   color: var(--text);
 }
 
@@ -1122,7 +1122,7 @@
   display: flex;
   align-items: center;
   justify-content: space-between;
-  color: #7a879d;
+  color: var(--muted);
 }
 
 .pagination {
@@ -1137,7 +1137,7 @@
   height: 32px;
   border: 0;
   border-radius: 8px;
-  background: #17243a;
+  background: var(--border);
   color: var(--muted);
   font-weight: 900;
 }
@@ -1148,7 +1148,7 @@
 
 .pagination .is-active {
   background: var(--cyan);
-  color: #031017;
+  color: var(--bg-deep);
 }
 
 .pagination__ellipsis {
@@ -1233,13 +1233,13 @@
   place-items: center;
   gap: 8px;
   border-radius: 8px;
-  background: #17243a;
+  background: var(--border);
   text-align: center;
 }
 
 .gap-card span,
 .gap-card small {
-  color: #8692a7;
+  color: var(--muted);
   font-size: 11px;
 }
 
@@ -1280,7 +1280,7 @@
   min-height: 38px;
   padding-top: 7px;
   border-radius: 4px;
-  color: #738096;
+  color: var(--muted);
   font-size: 10px;
   text-align: center;
 }
@@ -1295,7 +1295,7 @@
 }
 
 .gap-bars__item--3 {
-  background: rgba(16, 216, 242, 0.16);
+  background: rgba(var(--cyan-rgb), 0.16);
 }
 
 .gap-bars__item--4 {
@@ -1338,7 +1338,7 @@
 
 .role-stack__frontend {
   min-height: 170px;
-  background: rgba(16, 216, 242, 0.32);
+  background: rgba(var(--cyan-rgb), 0.32);
 }
 
 .role-stack__backend {
@@ -1373,7 +1373,7 @@
   display: grid;
   place-items: center;
   border-radius: 8px;
-  background: #101a2d;
+  background: var(--panel-strong);
   color: var(--cyan);
 }
 
@@ -1474,9 +1474,9 @@
   padding: 13px 14px;
   display: grid;
   gap: 5px;
-  border: 1px solid #24334d;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
 }
 
 .admin-detail-field span {
@@ -1496,9 +1496,9 @@
 
 .admin-tech-tags span {
   padding: 7px 9px;
-  border: 1px solid rgba(16, 216, 242, 0.22);
+  border: 1px solid rgba(var(--cyan-rgb), 0.22);
   border-radius: 999px;
-  background: rgba(16, 216, 242, 0.07);
+  background: rgba(var(--cyan-rgb), 0.07);
 }
 
 .admin-muted-text {
@@ -1513,9 +1513,9 @@
   display: flex;
   align-items: center;
   gap: 10px;
-  border: 1px solid #24334d;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
   color: var(--muted-strong);
   font-size: 13px;
   line-height: 1.5;
@@ -1545,9 +1545,9 @@
   align-items: center;
   justify-content: space-between;
   gap: 16px;
-  border: 1px solid #24334d;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
 }
 
 .admin-route-heading h3,
@@ -1579,9 +1579,9 @@
   display: grid;
   place-items: center;
   gap: 8px;
-  border: 1px solid #263650;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
   color: var(--cyan);
   text-align: center;
 }
@@ -1621,9 +1621,9 @@
   grid-template-columns: auto minmax(0, 1fr) auto;
   align-items: center;
   gap: 12px;
-  border: 1px solid #24334d;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
 }
 
 .admin-module-item__order {
@@ -1632,7 +1632,7 @@
   display: grid;
   place-items: center;
   border-radius: 50%;
-  background: rgba(16, 216, 242, 0.09);
+  background: rgba(var(--cyan-rgb), 0.09);
   color: var(--cyan);
   font-weight: 850;
 }
@@ -1682,9 +1682,9 @@
 .skill-gap-item {
   min-width: 0;
   padding: 15px;
-  border: 1px solid #24334d;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0b1524;
+  background: var(--panel-strong);
 }
 
 .analytics-bar-row__header,
@@ -1732,7 +1732,7 @@
   margin-top: 13px;
   overflow: hidden;
   border-radius: 999px;
-  background: #26354e;
+  background: var(--border);
 }
 
 .analytics-bar-row__track span,
@@ -1945,7 +1945,7 @@
   display: grid;
   place-items: center;
   border-radius: 10px;
-  background: rgba(16, 216, 242, 0.12);
+  background: rgba(var(--cyan-rgb), 0.12);
   color: var(--cyan);
   font-size: 18px;
 }
@@ -1991,13 +1991,13 @@
 }
 
 .settings-options button:hover {
-  border-color: rgba(16, 216, 242, 0.4);
+  border-color: rgba(var(--cyan-rgb), 0.4);
   color: var(--text);
 }
 
 .settings-options button.is-active {
   border-color: var(--cyan);
-  background: rgba(16, 216, 242, 0.1);
+  background: rgba(var(--cyan-rgb), 0.1);
   color: var(--text);
 }
 
diff --git a/frontend/src/styles/dashboard.css b/frontend/src/styles/dashboard.css
index aa1f16c..246ee46 100644
--- a/frontend/src/styles/dashboard.css
+++ b/frontend/src/styles/dashboard.css
@@ -61,7 +61,7 @@
   border: 1px solid rgba(255, 107, 122, 0.35);
   border-radius: 12px;
   background: rgba(255, 107, 122, 0.09);
-  color: #ffabb4;
+  color: var(--danger);
 }
 
 .student-dashboard-alert > svg {
@@ -116,9 +116,9 @@
   height: 44px;
   display: grid;
   place-items: center;
-  border: 1px solid rgba(16, 216, 242, 0.28);
+  border: 1px solid rgba(var(--cyan-rgb), 0.28);
   border-radius: 12px;
-  background: rgba(16, 216, 242, 0.08);
+  background: rgba(var(--cyan-rgb), 0.08);
   color: var(--cyan);
   font-size: 20px;
 }
@@ -220,8 +220,8 @@
   place-content: center;
   border-radius: 50%;
   background:
-    radial-gradient(circle at center, #101b2e 60%, transparent 61%),
-    conic-gradient(var(--cyan) var(--dashboard-progress), #27354c 0);
+    radial-gradient(circle at center, var(--bg-deep) 60%, transparent 61%),
+    conic-gradient(var(--cyan) var(--dashboard-progress), var(--border) 0);
   text-align: center;
 }
 
@@ -264,9 +264,9 @@
   align-items: center;
   justify-content: space-between;
   gap: 28px;
-  border: 1px solid rgba(16, 216, 242, 0.24);
+  border: 1px solid rgba(var(--cyan-rgb), 0.24);
   border-radius: 16px;
-  background: linear-gradient(125deg, rgba(16, 216, 242, 0.08), rgba(11, 20, 36, 0.98) 46%);
+  background: linear-gradient(125deg, rgba(var(--cyan-rgb), 0.08), rgba(11, 20, 36, 0.98) 46%);
 }
 
 .student-dashboard-next > div:first-child {
@@ -284,7 +284,7 @@
   place-items: center;
   border-radius: 12px;
   background: var(--cyan);
-  color: #06101d;
+  color: var(--bg-deep);
   font-size: 20px;
 }
 
@@ -323,12 +323,12 @@
 .student-dashboard-button--primary {
   border-color: var(--cyan);
   background: var(--cyan);
-  color: #06101d;
+  color: var(--bg-deep);
 }
 
 .student-dashboard-button--secondary {
   border-color: var(--border);
-  background: #0b1627;
+  background: var(--panel-strong);
   color: var(--muted-strong);
 }
 
@@ -362,7 +362,7 @@
 .student-dashboard-spinner {
   width: 38px;
   height: 38px;
-  border: 3px solid rgba(16, 216, 242, 0.16);
+  border: 3px solid rgba(var(--cyan-rgb), 0.16);
   border-top-color: var(--cyan);
   border-radius: 50%;
   animation: student-dashboard-spin 0.8s linear infinite;
diff --git a/frontend/src/styles/global.css b/frontend/src/styles/global.css
index 7184e62..5070289 100644
--- a/frontend/src/styles/global.css
+++ b/frontend/src/styles/global.css
@@ -5,20 +5,26 @@
   --panel: #121e32;
   --panel-soft: #17243a;
   --panel-strong: #0b1424;
+  --panel-strong-rgb: 11, 20, 36;
   --border: #223049;
   --text: #f7fbff;
   --muted: #96a3b7;
   --muted-strong: #c4ccda;
   --cyan: #10d8f2;
+  --cyan-rgb: 16, 216, 242;
   --cyan-strong: #05c7e6;
+  --on-accent: #06101d;
   --green: #22d09b;
+  --green-rgb: 34, 208, 155;
   --amber: #f6a400;
   --violet: #a77cff;
   --pink: #ee70b9;
   --danger: #ff6b7a;
+  --blue: #3486ff;
+  --on-muted: #dbe4ef;
   --radius-sm: 8px;
   --radius-card: 8px;
-  --focus-ring: 0 0 0 3px rgba(16, 216, 242, 0.22);
+  --focus-ring: 0 0 0 3px rgba(var(--cyan-rgb), 0.22);
   --shadow: 0 24px 70px rgba(0, 0, 0, 0.28);
   --shadow-soft: 0 16px 46px rgba(0, 0, 0, 0.16);
 }
@@ -30,17 +36,23 @@
   --panel: #ffffff;
   --panel-soft: #f3f6fb;
   --panel-strong: #ffffff;
+  --panel-strong-rgb: 255, 255, 255;
   --border: #d7deec;
   --text: #101828;
   --muted: #64748b;
   --muted-strong: #334155;
   --cyan: #0891a8;
+  --cyan-rgb: 8, 145, 168;
   --cyan-strong: #067a8f;
+  --on-accent: #ffffff;
   --green: #0f9d72;
+  --green-rgb: 15, 157, 114;
   --amber: #b4740a;
   --violet: #7c5cd6;
   --pink: #c94d92;
   --danger: #d6394a;
+  --blue: #2563eb;
+  --on-muted: #ffffff;
   --focus-ring: 0 0 0 3px rgba(8, 145, 168, 0.22);
   --shadow: 0 24px 60px rgba(16, 24, 40, 0.1);
   --shadow-soft: 0 12px 30px rgba(16, 24, 40, 0.08);
@@ -64,7 +76,7 @@ body {
 }
 
 ::selection {
-  background: rgba(16, 216, 242, 0.35);
+  background: rgba(var(--cyan-rgb), 0.35);
   color: var(--text);
 }
 
@@ -89,7 +101,7 @@ a {
 }
 
 :where(a, button, input, select, textarea, [tabindex]):focus-visible {
-  outline: 3px solid rgba(16, 216, 242, 0.7);
+  outline: 3px solid rgba(var(--cyan-rgb), 0.7);
   outline-offset: 3px;
 }
 
diff --git a/frontend/src/styles/perfil.css b/frontend/src/styles/perfil.css
index f51fed1..24a3e1a 100644
--- a/frontend/src/styles/perfil.css
+++ b/frontend/src/styles/perfil.css
@@ -47,8 +47,8 @@
 }
 
 .perfil-mode--edit {
-  border-color: rgba(16, 216, 242, 0.32);
-  background: rgba(16, 216, 242, 0.08);
+  border-color: rgba(var(--cyan-rgb), 0.32);
+  background: rgba(var(--cyan-rgb), 0.08);
   color: var(--cyan);
 }
 
@@ -91,9 +91,9 @@
   flex: 0 0 auto;
   display: grid;
   place-items: center;
-  border: 1px solid rgba(16, 216, 242, 0.28);
+  border: 1px solid rgba(var(--cyan-rgb), 0.28);
   border-radius: 12px;
-  background: rgba(16, 216, 242, 0.08);
+  background: rgba(var(--cyan-rgb), 0.08);
   color: var(--cyan);
   font-size: 19px;
 }
@@ -127,14 +127,14 @@
   border: 1px solid var(--border);
   border-radius: 10px;
   outline: none;
-  background: #091321;
+  background: var(--bg-deep);
   color: var(--text);
   transition: border-color 0.18s ease, box-shadow 0.18s ease;
 }
 
 .perfil-field input:focus {
   border-color: var(--cyan);
-  box-shadow: 0 0 0 3px rgba(16, 216, 242, 0.1);
+  box-shadow: 0 0 0 3px rgba(var(--cyan-rgb), 0.1);
 }
 
 .perfil-field input[aria-invalid="true"] {
@@ -166,7 +166,7 @@
   gap: 8px;
   border: 1px solid var(--border);
   border-radius: 12px;
-  background: #0b1627;
+  background: var(--panel-strong);
   color: var(--text);
   text-align: left;
   transition: border-color 0.18s ease, background-color 0.18s ease, transform 0.18s ease;
@@ -174,15 +174,15 @@
 
 .perfil-level:hover,
 .perfil-category:hover {
-  border-color: #3b526f;
-  background: #0f1d31;
+  border-color: var(--muted);
+  background: var(--panel-strong);
   transform: translateY(-1px);
 }
 
 .perfil-level--selected,
 .perfil-category--selected {
-  border-color: rgba(16, 216, 242, 0.68);
-  background: rgba(16, 216, 242, 0.08);
+  border-color: rgba(var(--cyan-rgb), 0.68);
+  background: rgba(var(--cyan-rgb), 0.08);
 }
 
 .perfil-level strong,
@@ -207,9 +207,9 @@
   height: 22px;
   display: grid;
   place-items: center;
-  border: 1px solid #40516a;
+  border: 1px solid var(--muted);
   border-radius: 999px;
-  color: #07111f;
+  color: var(--bg-deep);
   font-size: 14px;
 }
 
@@ -221,7 +221,7 @@
 
 .perfil-field-error {
   margin: 10px 0 0;
-  color: #ff9aa5;
+  color: var(--danger);
   font-size: 13px;
   font-weight: 650;
 }
@@ -249,13 +249,13 @@
 .perfil-feedback--error {
   border-color: rgba(255, 107, 122, 0.35);
   background: rgba(255, 107, 122, 0.09);
-  color: #ff9aa5;
+  color: var(--danger);
 }
 
 .perfil-feedback--success {
   border-color: rgba(34, 208, 155, 0.35);
   background: rgba(34, 208, 155, 0.09);
-  color: #71e6bd;
+  color: var(--green);
 }
 
 .perfil-actions {
@@ -266,7 +266,7 @@
   gap: 18px;
   border: 1px solid var(--border);
   border-radius: 14px;
-  background: #0a1424;
+  background: var(--panel-strong);
 }
 
 .perfil-actions p {
@@ -286,14 +286,14 @@
   border: 1px solid var(--cyan);
   border-radius: 9px;
   background: var(--cyan);
-  color: #06101d;
+  color: var(--bg-deep);
   font-weight: 850;
 }
 
 .perfil-actions button:disabled {
-  border-color: #40516a;
-  background: #40516a;
-  color: #9ca9bb;
+  border-color: var(--muted);
+  background: var(--muted);
+  color: var(--muted);
 }
 
 .perfil-state {
@@ -333,7 +333,7 @@
 .perfil-spinner {
   width: 38px;
   height: 38px;
-  border: 3px solid rgba(16, 216, 242, 0.16);
+  border: 3px solid rgba(var(--cyan-rgb), 0.16);
   border-top-color: var(--cyan);
   border-radius: 999px;
   animation: perfil-spin 0.8s linear infinite;
diff --git a/frontend/src/styles/ruta.css b/frontend/src/styles/ruta.css
index ee17eb6..9191ad4 100644
--- a/frontend/src/styles/ruta.css
+++ b/frontend/src/styles/ruta.css
@@ -57,13 +57,13 @@
 .route-feedback--error {
   border-color: rgba(255, 107, 122, 0.35);
   background: rgba(255, 107, 122, 0.09);
-  color: #ff9aa5;
+  color: var(--danger);
 }
 
 .route-feedback--success {
   border-color: rgba(34, 208, 155, 0.35);
   background: rgba(34, 208, 155, 0.09);
-  color: #71e6bd;
+  color: var(--green);
 }
 
 .route-empty-card,
@@ -91,9 +91,9 @@
   height: 58px;
   display: grid;
   place-items: center;
-  border: 1px solid rgba(16, 216, 242, 0.3);
+  border: 1px solid rgba(var(--cyan-rgb), 0.3);
   border-radius: 16px;
-  background: rgba(16, 216, 242, 0.09);
+  background: rgba(var(--cyan-rgb), 0.09);
   color: var(--cyan);
   font-size: 25px;
 }
@@ -158,7 +158,7 @@
   grid-template-columns: auto minmax(0, 1fr) auto;
   align-items: center;
   gap: 18px;
-  background: rgba(16, 216, 242, 0.035);
+  background: rgba(var(--cyan-rgb), 0.035);
 }
 
 .route-ready-card__action h3 {
@@ -172,7 +172,7 @@
   border: 1px solid var(--border);
   border-radius: 16px;
   background:
-    radial-gradient(circle at top right, rgba(16, 216, 242, 0.08), transparent 34%),
+    radial-gradient(circle at top right, rgba(var(--cyan-rgb), 0.08), transparent 34%),
     linear-gradient(145deg, rgba(18, 30, 50, 0.98), rgba(11, 20, 36, 0.98));
   box-shadow: 0 16px 46px rgba(0, 0, 0, 0.16);
 }
@@ -212,7 +212,7 @@
   border: 1px solid rgba(246, 164, 0, 0.28);
   border-radius: 9px;
   background: rgba(246, 164, 0, 0.07);
-  color: #f9c65e;
+  color: var(--amber);
   font-size: 13px;
 }
 
@@ -229,7 +229,7 @@
   display: flex;
   align-items: center;
   gap: 13px;
-  border: 1px solid #263650;
+  border: 1px solid var(--border);
   border-radius: 12px;
   background: rgba(8, 19, 33, 0.72);
 }
@@ -241,7 +241,7 @@
   display: grid;
   place-items: center;
   border-radius: 10px;
-  background: rgba(16, 216, 242, 0.09);
+  background: rgba(var(--cyan-rgb), 0.09);
   color: var(--cyan);
   font-size: 19px;
 }
@@ -277,7 +277,7 @@
 .route-progress-bar {
   margin-top: 22px;
   padding: 17px;
-  border: 1px solid #263650;
+  border: 1px solid var(--border);
   border-radius: 12px;
   background: rgba(8, 19, 33, 0.55);
 }
@@ -302,15 +302,15 @@
   height: 11px;
   overflow: hidden;
   border-radius: 999px;
-  background: #1b2b41;
+  background: var(--border);
 }
 
 .route-progress-bar__track span {
   height: 100%;
   display: block;
   border-radius: inherit;
-  background: linear-gradient(90deg, #11bad2, var(--cyan), #22d09b);
-  box-shadow: 0 0 16px rgba(16, 216, 242, 0.36);
+  background: linear-gradient(90deg, var(--cyan), var(--cyan), var(--green));
+  box-shadow: 0 0 16px rgba(var(--cyan-rgb), 0.36);
   transition: width 0.35s ease;
 }
 
@@ -328,7 +328,7 @@
 .route-progress-hours {
   display: grid;
   grid-template-columns: repeat(3, minmax(0, 1fr));
-  border: 1px solid #263650;
+  border: 1px solid var(--border);
   border-radius: 12px;
   background: rgba(8, 19, 33, 0.55);
 }
@@ -339,7 +339,7 @@
 }
 
 .route-progress-hours > div + div {
-  border-left: 1px solid #263650;
+  border-left: 1px solid var(--border);
 }
 
 .route-progress-hours span {
@@ -375,7 +375,7 @@
 
 .route-next-module__content > span,
 .route-next-module--complete > div > span {
-  color: #c4a9ff;
+  color: var(--violet);
   font-size: 10px;
   font-weight: 850;
   letter-spacing: 0.06em;
@@ -410,14 +410,14 @@
   border: 1px solid rgba(167, 124, 255, 0.5);
   border-radius: 8px;
   background: rgba(167, 124, 255, 0.14);
-  color: #d3c0ff;
+  color: var(--violet);
   font-size: 12px;
   font-weight: 850;
 }
 
 .route-next-module button:hover,
 .route-next-module button:focus-visible {
-  border-color: #c4a9ff;
+  border-color: var(--violet);
   background: rgba(167, 124, 255, 0.22);
 }
 
@@ -506,9 +506,9 @@
   display: flex;
   align-items: center;
   gap: 15px;
-  border: 1px solid rgba(16, 216, 242, 0.28);
+  border: 1px solid rgba(var(--cyan-rgb), 0.28);
   border-radius: 14px;
-  background: rgba(16, 216, 242, 0.07);
+  background: rgba(var(--cyan-rgb), 0.07);
 }
 
 .route-working p {
@@ -570,7 +570,7 @@
   width: 9px;
   height: 9px;
   border-radius: 50%;
-  background: #96a3b7;
+  background: var(--muted);
   content: "";
 }
 
@@ -588,7 +588,7 @@
   overflow: hidden;
   border: 1px solid var(--border);
   border-radius: 16px;
-  background: #081321;
+  background: var(--bg-deep);
   box-shadow: 0 16px 46px rgba(0, 0, 0, 0.16);
 }
 
@@ -607,19 +607,19 @@
   width: 250px;
   min-height: 124px;
   padding: 15px;
-  border: 1px solid #536178;
-  border-left: 5px solid #96a3b7;
+  border: 1px solid var(--muted);
+  border-left: 5px solid var(--muted);
   border-radius: 13px;
-  background: linear-gradient(145deg, #15233a, #0d192b);
+  background: linear-gradient(145deg, var(--border), var(--bg-deep));
   color: var(--text);
   box-shadow: 0 12px 28px rgba(0, 0, 0, 0.3);
   cursor: pointer;
 }
 
 .route-flow-node--selected {
-  outline: 3px solid rgba(16, 216, 242, 0.72);
+  outline: 3px solid rgba(var(--cyan-rgb), 0.72);
   outline-offset: 4px;
-  box-shadow: 0 0 0 7px rgba(16, 216, 242, 0.1), 0 16px 34px rgba(0, 0, 0, 0.38);
+  box-shadow: 0 0 0 7px rgba(var(--cyan-rgb), 0.1), 0 16px 34px rgba(0, 0, 0, 0.38);
 }
 
 .route-flow-node--en_progreso {
@@ -688,17 +688,17 @@
 .route-flow-node .react-flow__handle {
   width: 8px;
   height: 8px;
-  border: 2px solid #081321;
+  border: 2px solid var(--bg-deep);
   background: var(--cyan);
 }
 
 .route-roadmap .react-flow__edge-path {
-  stroke: #71819a;
+  stroke: var(--muted);
   stroke-width: 2;
 }
 
 .route-roadmap .route-flow-edge--primary .react-flow__edge-path {
-  stroke: rgba(16, 216, 242, 0.78);
+  stroke: rgba(var(--cyan-rgb), 0.78);
   stroke-width: 2.2;
 }
 
@@ -707,14 +707,14 @@
 }
 
 .route-roadmap .route-flow-edge--secondary .react-flow__edge-path {
-  stroke: #8795aa;
+  stroke: var(--muted);
   stroke-dasharray: 6 6;
   stroke-width: 1.5;
 }
 
 .route-roadmap .react-flow__arrowhead polyline {
-  stroke: #71819a;
-  fill: #71819a;
+  stroke: var(--muted);
+  fill: var(--muted);
 }
 
 .route-roadmap .react-flow__controls {
@@ -726,12 +726,12 @@
 
 .route-roadmap .react-flow__controls-button {
   border-bottom-color: var(--border);
-  background: #101e32;
+  background: var(--panel-strong);
   fill: var(--muted-strong);
 }
 
 .route-roadmap .react-flow__controls-button:hover {
-  background: #192a43;
+  background: var(--border);
 }
 
 .route-modules-heading {
@@ -771,8 +771,8 @@
 }
 
 .route-module--selected {
-  border-color: rgba(16, 216, 242, 0.7);
-  box-shadow: 0 0 0 3px rgba(16, 216, 242, 0.08);
+  border-color: rgba(var(--cyan-rgb), 0.7);
+  box-shadow: 0 0 0 3px rgba(var(--cyan-rgb), 0.08);
 }
 
 .route-module__number {
@@ -780,9 +780,9 @@
   height: 38px;
   display: grid;
   place-items: center;
-  border: 1px solid rgba(16, 216, 242, 0.3);
+  border: 1px solid rgba(var(--cyan-rgb), 0.3);
   border-radius: 50%;
-  background: rgba(16, 216, 242, 0.08);
+  background: rgba(var(--cyan-rgb), 0.08);
   color: var(--cyan);
   font-weight: 850;
 }
@@ -806,7 +806,7 @@
   border: 1px solid rgba(167, 124, 255, 0.3);
   border-radius: 999px;
   background: rgba(167, 124, 255, 0.08);
-  color: #c4a9ff;
+  color: var(--violet);
   font-size: 10px;
   font-weight: 850;
   letter-spacing: 0.05em;
@@ -857,9 +857,9 @@
   display: inline-flex;
   align-items: center;
   gap: 5px;
-  border: 1px solid rgba(16, 216, 242, 0.28);
+  border: 1px solid rgba(var(--cyan-rgb), 0.28);
   border-radius: 8px;
-  background: rgba(16, 216, 242, 0.06);
+  background: rgba(var(--cyan-rgb), 0.06);
   color: var(--cyan);
   font-size: 12px;
   font-weight: 800;
@@ -867,8 +867,8 @@
 
 .route-module__open:hover,
 .route-module__open:focus-visible {
-  border-color: rgba(16, 216, 242, 0.7);
-  background: rgba(16, 216, 242, 0.12);
+  border-color: rgba(var(--cyan-rgb), 0.7);
+  background: rgba(var(--cyan-rgb), 0.12);
 }
 
 .route-resources {
@@ -897,15 +897,15 @@
   grid-template-columns: auto minmax(0, 1fr) auto auto;
   align-items: center;
   gap: 9px;
-  border: 1px solid #263650;
+  border: 1px solid var(--border);
   border-radius: 9px;
-  background: #0a1525;
+  background: var(--bg-deep);
   color: var(--muted-strong);
   font-size: 13px;
 }
 
 .route-resources a.route-resource:hover {
-  border-color: rgba(16, 216, 242, 0.42);
+  border-color: rgba(var(--cyan-rgb), 0.42);
   color: var(--text);
 }
 
@@ -949,7 +949,7 @@
   display: flex;
   flex-direction: column;
   border-left: 1px solid var(--border);
-  background: #091423;
+  background: var(--bg-deep);
   box-shadow: -24px 0 60px rgba(0, 0, 0, 0.42);
   animation: route-detail-slide 0.24s ease-out;
 }
@@ -987,7 +987,7 @@
   place-items: center;
   border: 1px solid var(--border);
   border-radius: 10px;
-  background: #101e32;
+  background: var(--panel-strong);
   color: var(--muted-strong);
   font-size: 20px;
 }
@@ -1020,9 +1020,9 @@
   display: inline-flex;
   align-items: center;
   gap: 6px;
-  border: 1px solid #2a3a53;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0e1b2e;
+  background: var(--bg-deep);
   color: var(--muted-strong);
   font-size: 12px;
 }
@@ -1058,7 +1058,7 @@
   display: grid;
   place-items: center;
   border-radius: 50%;
-  background: rgba(16, 216, 242, 0.1);
+  background: rgba(var(--cyan-rgb), 0.1);
   color: var(--cyan);
   font-size: 12px;
 }
@@ -1105,9 +1105,9 @@
   align-items: center;
   justify-content: center;
   gap: 6px;
-  border: 1px solid #35445a;
+  border: 1px solid var(--border);
   border-radius: 8px;
-  background: #0d1a2c;
+  background: var(--bg-deep);
   color: var(--muted-strong);
   font-size: 12px;
   font-weight: 800;
@@ -1137,8 +1137,8 @@
 }
 
 .route-module-detail__status-button[aria-pressed="true"] {
-  border-color: rgba(16, 216, 242, 0.55);
-  background: rgba(16, 216, 242, 0.1);
+  border-color: rgba(var(--cyan-rgb), 0.55);
+  background: rgba(var(--cyan-rgb), 0.1);
   color: var(--cyan);
   opacity: 1;
 }
@@ -1155,7 +1155,7 @@
 .route-module-detail__saving .route-button__spinner {
   width: 15px;
   height: 15px;
-  border-color: rgba(16, 216, 242, 0.2);
+  border-color: rgba(var(--cyan-rgb), 0.2);
   border-top-color: var(--cyan);
 }
 
@@ -1179,13 +1179,13 @@
 .route-module-detail__feedback--success {
   border-color: rgba(34, 208, 155, 0.35);
   background: rgba(34, 208, 155, 0.08);
-  color: #71e6bd;
+  color: var(--green);
 }
 
 .route-module-detail__feedback--error {
   border-color: rgba(255, 107, 122, 0.35);
   background: rgba(255, 107, 122, 0.08);
-  color: #ff9aa5;
+  color: var(--danger);
 }
 
 .route-module-detail__resources {
@@ -1201,15 +1201,15 @@
   grid-template-columns: auto minmax(0, 1fr) auto;
   align-items: center;
   gap: 11px;
-  border: 1px solid #293a54;
+  border: 1px solid var(--border);
   border-radius: 10px;
-  background: #0c192a;
+  background: var(--bg-deep);
   color: var(--muted-strong);
 }
 
 a.route-module-detail__resource:hover,
 a.route-module-detail__resource:focus-visible {
-  border-color: rgba(16, 216, 242, 0.52);
+  border-color: rgba(var(--cyan-rgb), 0.52);
   color: var(--text);
 }
 
@@ -1219,7 +1219,7 @@ a.route-module-detail__resource:focus-visible {
   display: grid;
   place-items: center;
   border-radius: 9px;
-  background: rgba(16, 216, 242, 0.08);
+  background: rgba(var(--cyan-rgb), 0.08);
   color: var(--cyan);
 }
 
@@ -1257,7 +1257,7 @@ a.route-module-detail__resource:focus-visible {
 .route-module-detail__empty {
   margin: 14px 0 0;
   padding: 14px;
-  border: 1px dashed #31425b;
+  border: 1px dashed var(--border);
   border-radius: 10px;
   color: var(--muted);
   font-size: 13px;
@@ -1338,7 +1338,7 @@ a.route-module-detail__resource:focus-visible {
   width: 38px;
   height: 38px;
   flex: 0 0 auto;
-  border: 3px solid rgba(16, 216, 242, 0.16);
+  border: 3px solid rgba(var(--cyan-rgb), 0.16);
   border-top-color: var(--cyan);
   border-radius: 50%;
   animation: route-spin 0.8s linear infinite;
@@ -1450,7 +1450,7 @@ a.route-module-detail__resource:focus-visible {
   }
 
   .route-progress-hours > div + div {
-    border-top: 1px solid #263650;
+    border-top: 1px solid var(--border);
     border-left: 0;
   }
 
diff --git a/frontend/src/styles/ui.css b/frontend/src/styles/ui.css
index 3a26a68..591685b 100644
--- a/frontend/src/styles/ui.css
+++ b/frontend/src/styles/ui.css
@@ -14,7 +14,7 @@
   height: 38px;
   flex: 0 0 auto;
   display: inline-block;
-  border: 3px solid rgba(16, 216, 242, 0.16);
+  border: 3px solid rgba(var(--cyan-rgb), 0.16);
   border-top-color: var(--cyan);
   border-radius: 999px;
   animation: ui-spin 0.8s linear infinite;
@@ -38,7 +38,7 @@
   border-radius: var(--radius-sm);
   background:
     linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.08), transparent),
-    #14233a;
+    var(--panel-soft);
   background-size: 220% 100%;
   animation: ui-skeleton 1.2s ease-in-out infinite;
 }
@@ -118,20 +118,20 @@
 .route-button--primary {
   border-color: var(--cyan);
   background: var(--cyan);
-  color: #06101d;
+  color: var(--on-accent);
 }
 
 .route-button--secondary,
 .route-button--ghost {
   border-color: var(--border);
-  background: #0b1627;
+  background: var(--panel-soft);
   color: var(--muted-strong);
 }
 
 .route-button--danger {
   border-color: var(--danger);
   background: var(--danger);
-  color: #16070a;
+  color: var(--on-accent);
 }
 
 .route-button:hover:not(:disabled) {
@@ -139,16 +139,16 @@
 }
 
 .route-button:disabled {
-  border-color: #40516a;
-  background: #40516a;
-  color: #9ca9bb;
+  border-color: var(--border);
+  background: var(--panel-soft);
+  color: var(--muted);
 }
 
 .route-button__spinner {
   width: 17px;
   height: 17px;
   border: 2px solid rgba(6, 16, 29, 0.24);
-  border-top-color: #06101d;
+  border-top-color: var(--on-accent);
   border-radius: 50%;
   animation: ui-spin 0.8s linear infinite;
 }
@@ -173,9 +173,9 @@
   height: 58px;
   display: grid;
   place-items: center;
-  border: 1px solid rgba(16, 216, 242, 0.3);
+  border: 1px solid rgba(var(--cyan-rgb), 0.3);
   border-radius: var(--radius-card);
-  background: rgba(16, 216, 242, 0.09);
+  background: rgba(var(--cyan-rgb), 0.09);
   color: var(--cyan);
   font-size: 25px;
 }
@@ -242,11 +242,11 @@
   grid-template-columns: auto minmax(0, 1fr) auto;
   align-items: start;
   gap: 12px;
-  border: 1px solid rgba(16, 216, 242, 0.28);
+  border: 1px solid rgba(var(--cyan-rgb), 0.28);
   border-radius: var(--radius-card);
-  background: rgba(9, 20, 35, 0.96);
+  background: rgba(var(--panel-strong-rgb), 0.96);
   color: var(--text);
-  box-shadow: 0 22px 50px rgba(0, 0, 0, 0.28);
+  box-shadow: var(--shadow);
   pointer-events: auto;
   animation: toast-in 0.18s ease-out;
 }
@@ -367,7 +367,7 @@
   align-items: center;
   justify-content: center;
   border-radius: 999px;
-  background: linear-gradient(145deg, rgba(16, 216, 242, 0.24), rgba(167, 124, 255, 0.24));
+  background: linear-gradient(145deg, rgba(var(--cyan-rgb), 0.24), rgba(167, 124, 255, 0.24));
   color: var(--cyan);
   font-weight: 800;
   letter-spacing: 0.02em;
-- 
2.43.0

