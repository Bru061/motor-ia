From 424b133903447075af2f8daaa6883208f06230b4 Mon Sep 17 00:00:00 2001
From: Bru <bru@motoria.local>
Date: Fri, 24 Jul 2026 02:19:49 +0000
Subject: [PATCH 2/3] feat(theme): agregar sistema de tema claro/oscuro con
 persistencia

---
 frontend/src/context/ThemeContext.jsx | 37 +++++++++++++++++++++++++++
 frontend/src/context/themeContext.js  |  3 +++
 frontend/src/hooks/useTheme.js        | 14 ++++++++++
 frontend/src/main.jsx                 | 13 ++++++----
 frontend/src/styles/global.css        | 23 +++++++++++++++++
 5 files changed, 85 insertions(+), 5 deletions(-)
 create mode 100644 frontend/src/context/ThemeContext.jsx
 create mode 100644 frontend/src/context/themeContext.js
 create mode 100644 frontend/src/hooks/useTheme.js

diff --git a/frontend/src/context/ThemeContext.jsx b/frontend/src/context/ThemeContext.jsx
new file mode 100644
index 0000000..a97a540
--- /dev/null
+++ b/frontend/src/context/ThemeContext.jsx
@@ -0,0 +1,37 @@
+import { useCallback, useEffect, useMemo, useState } from "react";
+import { ThemeContext } from "./themeContext";
+
+const STORAGE_KEY = "motoria_theme";
+
+function getInitialTheme() {
+  if (typeof window === "undefined") {
+    return "dark";
+  }
+
+  const stored = window.localStorage.getItem(STORAGE_KEY);
+  return stored === "light" ? "light" : "dark";
+}
+
+function ThemeProvider({ children }) {
+  const [theme, setTheme] = useState(getInitialTheme);
+
+  useEffect(() => {
+    document.documentElement.setAttribute("data-theme", theme);
+    window.localStorage.setItem(STORAGE_KEY, theme);
+  }, [theme]);
+
+  const setLightTheme = useCallback(() => setTheme("light"), []);
+  const setDarkTheme = useCallback(() => setTheme("dark"), []);
+  const toggleTheme = useCallback(() => {
+    setTheme((current) => (current === "dark" ? "light" : "dark"));
+  }, []);
+
+  const value = useMemo(
+    () => ({ theme, setLightTheme, setDarkTheme, toggleTheme }),
+    [theme, setLightTheme, setDarkTheme, toggleTheme],
+  );
+
+  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
+}
+
+export default ThemeProvider;
diff --git a/frontend/src/context/themeContext.js b/frontend/src/context/themeContext.js
new file mode 100644
index 0000000..c1165ec
--- /dev/null
+++ b/frontend/src/context/themeContext.js
@@ -0,0 +1,3 @@
+import { createContext } from "react";
+
+export const ThemeContext = createContext(null);
diff --git a/frontend/src/hooks/useTheme.js b/frontend/src/hooks/useTheme.js
new file mode 100644
index 0000000..768037b
--- /dev/null
+++ b/frontend/src/hooks/useTheme.js
@@ -0,0 +1,14 @@
+import { useContext } from "react";
+import { ThemeContext } from "../context/themeContext";
+
+function useTheme() {
+  const context = useContext(ThemeContext);
+
+  if (!context) {
+    throw new Error("useTheme debe usarse dentro de ThemeProvider");
+  }
+
+  return context;
+}
+
+export default useTheme;
diff --git a/frontend/src/main.jsx b/frontend/src/main.jsx
index 0409a32..61fb0f7 100644
--- a/frontend/src/main.jsx
+++ b/frontend/src/main.jsx
@@ -2,6 +2,7 @@ import React from "react";
 import ReactDOM from "react-dom/client";
 import App from "./App.jsx";
 import AuthProvider from "./context/AuthContext.jsx";
+import ThemeProvider from "./context/ThemeContext.jsx";
 import ToastProvider from "./context/ToastContext.jsx";
 import "reactflow/dist/style.css";
 import "./styles/global.css";
@@ -10,10 +11,12 @@ import "./styles/Pages.css";
 
 ReactDOM.createRoot(document.getElementById("root")).render(
   <React.StrictMode>
-    <AuthProvider>
-      <ToastProvider>
-        <App />
-      </ToastProvider>
-    </AuthProvider>
+    <ThemeProvider>
+      <AuthProvider>
+        <ToastProvider>
+          <App />
+        </ToastProvider>
+      </AuthProvider>
+    </ThemeProvider>
   </React.StrictMode>
 );
diff --git a/frontend/src/styles/global.css b/frontend/src/styles/global.css
index 56a10d3..7184e62 100644
--- a/frontend/src/styles/global.css
+++ b/frontend/src/styles/global.css
@@ -23,6 +23,29 @@
   --shadow-soft: 0 16px 46px rgba(0, 0, 0, 0.16);
 }
 
+:root[data-theme="light"] {
+  color-scheme: light;
+  --bg: #eef2f8;
+  --bg-deep: #e4e9f2;
+  --panel: #ffffff;
+  --panel-soft: #f3f6fb;
+  --panel-strong: #ffffff;
+  --border: #d7deec;
+  --text: #101828;
+  --muted: #64748b;
+  --muted-strong: #334155;
+  --cyan: #0891a8;
+  --cyan-strong: #067a8f;
+  --green: #0f9d72;
+  --amber: #b4740a;
+  --violet: #7c5cd6;
+  --pink: #c94d92;
+  --danger: #d6394a;
+  --focus-ring: 0 0 0 3px rgba(8, 145, 168, 0.22);
+  --shadow: 0 24px 60px rgba(16, 24, 40, 0.1);
+  --shadow-soft: 0 12px 30px rgba(16, 24, 40, 0.08);
+}
+
 * {
   box-sizing: border-box;
 }
-- 
2.43.0

